import 'package:flutter/material.dart';

class ChartWeekDays extends StatelessWidget {
  const ChartWeekDays({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}