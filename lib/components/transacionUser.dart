import 'package:flutter/material.dart';

class TransacionUser extends StatelessWidget {
  const TransacionUser({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}